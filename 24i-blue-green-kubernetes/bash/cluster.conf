# /etc/casino/cluster.conf
# Environment: production
# Do NOT store actual secret values here — use Vault references in the script

CLUSTER_API_HOST="10.0.10.20"           # green cluster API (changes per rotation)
CLUSTER_WORKER_HOSTS="10.0.10.21 10.0.10.22 10.0.10.23"
CLUSTER_CIDR_OCTET="42"                    # 10.42.0.0/16 for pods, 10.43.0.0/16 for services
VAULT_ADDR="https://10.0.10.102:8200"
VAULT_ADDR_IP="10.0.10.102"
REGISTRY_HOST="registry.casino.internal:5000"
IMAGE_TAG="${IMAGE_TAG:-latest}"
KUBECONFIG_OUTPUT="/root/.kube/casino-green.yaml"
POSTGRES_HOST="10.0.10.100"
REDIS_HOST="10.0.10.101"

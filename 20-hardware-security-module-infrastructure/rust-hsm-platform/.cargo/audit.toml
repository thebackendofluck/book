[advisories]
# RUSTSEC-2023-0071 (rsa "Marvin" timing sidechannel): no patched release
# exists upstream (rsa 0.9.x). Sample project, not network-exposed; revisit
# when rsa ships a fix.
ignore = ["RUSTSEC-2023-0071"]

# ---------------------------------------------------------------------------
# Runtime dependencies
# ---------------------------------------------------------------------------

# AWS SDK — must be listed explicitly for Lambda packaging
boto3>=1.34.0
botocore>=1.43.8

# MaxMind GeoIP2 ASN database (optional — install only when MAXMIND_DB_PATH
# points to a valid .mmdb file; Lambda layer is the recommended delivery path)
geoip2>=4.8.0
maxminddb>=2.6.0

# ---------------------------------------------------------------------------
# Test dependencies (not deployed to Lambda)
# ---------------------------------------------------------------------------

# Moto: AWS service mocking for unit tests
moto[all]>=5.2.1

# Test runner and plugins
pytest>=9.0.3
pytest-cov>=5.0.0

# Type checking (used by ty check per project conventions)
# ty is installed separately via: pip install ty

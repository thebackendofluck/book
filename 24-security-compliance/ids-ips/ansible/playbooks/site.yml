---
# Companion code for "The Backend of Luck" - Chapter 24, Security and Compliance.
# https://thebackendofluck.com | https://github.com/thebackendofluck/book
# SPDX-License-Identifier: Apache-2.0
#
# FOR TESTING AND EVALUATION ONLY. NOT FOR PRODUCTION USE.
# Published to demonstrate the patterns explained in the book. This code is
# not certified for real-money gaming: operating a gambling platform requires
# your own licence, independent test-lab certification (GLI, eCOGRA or
# equivalent) and regulator approval.

# playbooks/site.yml
# Master playbook — includes all component playbooks.
# Run this for a full initial deployment or idempotent re-apply.
#
# Usage:
#   ansible-playbook -i inventory/production.yml playbooks/site.yml
#   ansible-playbook -i inventory/production.yml playbooks/site.yml --tags configure
#   ansible-playbook -i inventory/production.yml playbooks/site.yml --limit ids-dmz-01.prod.igaming.internal

- name: Deploy Suricata IDS/IPS across all sensor zones
  ansible.builtin.import_playbook: deploy_suricata.yml
